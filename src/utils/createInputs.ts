// import InputField from '../components/InputField/InputField';
// import { profileGeneralData } from '../data/data';

// export function createInputs(data: typeof profileGeneralData) {
//   const inputFields = [];
//   for (const key in data) {
//     const field = new InputField(
//       data[key as keyof typeof data].labelText,
//       data[key as keyof typeof data].clueText,
//       data[key as keyof typeof data].reg,
//     );
//     inputFields.push([key, field]);
//   }

//   const result = Object.fromEntries(inputFields);
//   return result;
// }
